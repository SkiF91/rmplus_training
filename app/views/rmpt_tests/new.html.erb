<% if action_name.to_s != 'new' %>
  <script type="text/javascript" charset="utf-8">
    if ("replaceState" in window.history) {
      window.history.replaceState(null, document.title, '<%= Redmine::Utils.relative_url_root %>/rmpt_tests/new');
    }
  </script>
<% end %>

<%= render partial: 'form' %>